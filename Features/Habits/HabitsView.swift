import SwiftUI

struct HabitsView: View {
    var body: some View {
        NavigationStack {
            Text("Habits")
                .navigationTitle("Habits")
        }
    }
}

#Preview {
    HabitsView()
}
